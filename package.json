{
  "name": "gold-silver-rate",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . gold-silver-rate --platform=win32 --arch=x64 --icon=icon.ico --out=dist --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^37.2.6"
  },
  "build": {
  "files": [
    "!node_modules/**/*",
    "dist/**/*",
    "main.js",
    "package.json"
  ],
  "extraResources": [
    {
      "from": "assets/",
      "to": "assets",
      "filter": ["**/*"]
    }
  ]
}

}
